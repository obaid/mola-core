# claude-code adapter

Set `BENCHMARK_AGENT_COMMAND` to this external agent's non-interactive command.
It receives the canonical prompt on stdin plus `MOLA_MACHINE`. Configure Mola
MCP/API access outside this repository. Record the agent and model versions when
known; report unavailable metrics as unavailable.
