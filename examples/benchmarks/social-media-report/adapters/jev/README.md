# Jev adapter contract

Jev is a bounded decision layer, not a report-writing model. Use the runnable
`../../../jev/fast-computer-loop/` example to select from typed candidate
computer actions, then pair it with an explicitly named writer if you want to
compare a full report workflow. Record both components and their versions.
