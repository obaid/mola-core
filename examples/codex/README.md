# codex with Mola

Configure this external agent to run `npx -y mola-core mcp`, or give it the
REST/action-session example as a tool. Create one computer, retain its ID across
tasks, stop it when idle, and obtain a new session after every boot. The agent
continues to own planning, memory and model selection; Mola owns the computer.
